# (Credit flow control disabled via advanced.config)
# Remove flow_control.disable here to avoid unknown setting error in new config format

# Set memory high watermark to 100% to avoid premature memory-based throttling.
# This allows the broker to use up to its container memory limit before blocking.
vm_memory_high_watermark.relative = 1.0